spring:
  application:
    name: studentAppSpringDetails
  profiles:
    active: dev
---
spring:
  cloud:
    config:
      server:
        bootstrap: false
        git:
          uri: https://github.com/yogeshhpatil/cloud-config.git

# bootstrap: false -> take local profiles :: true -> check git repo for profiles

